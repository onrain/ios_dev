- if notice
  #notice{style:"position:relative; top:-20px;"}=notice
  
%h1 Listing clients

%table{class:"table table-striped"}
  %tr
    %th ID
    %th Company Name
    %th Name
    %th Email
    %th Handle
    %th
      %i{class:"icon-file"}
    %th
      %i{class:"icon-wrench"}
    %th
      %i{class:"icon-trash"}

  - @clients.each do |client|
    %tr
      %td= link_to client.id, client_path(client.id)
      %td= check_company(client.company_name, client.company_id).html_safe
      %td= client.name
      %td= client.email
      %td= client.handle
      %td= link_to 'Show', client, class:"btn btn-small btn-info"
      %td= link_to 'Edit', edit_client_path(client), class:"btn btn-success btn-small"
      %td= link_to 'Destroy', client, :confirm => 'Are you sure?', :method => :delete, class:" btn btn-danger btn-small"

%br

= link_to 'New Client', new_client_path, class:"btn btn-inverse btn-small"
