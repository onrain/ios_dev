%div{class:"well well-small", align:"center"}
  %h1 Editing client

= render 'form'

%div{class:"well well-small"}  
  .btn-group
    = link_to 'Show', @client, class:"btn"
    = link_to 'Back', back(clients_path), class:"btn"
