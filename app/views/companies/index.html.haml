%h1 Listing companies

%table{class:"table table-striped"}
  %tr
    %th Name
    %th Website
    %th
    %th
    %th

  - @companies.each do |company|
    %tr
      %td= company.name
      %td= company.website
      %td{style:"width:100px;"}= link_to 'Show', company, class:"btn btn-small btn-info"
      %td{style:"width:100px;"}= link_to 'Edit', edit_company_path(company), class:"btn btn-success btn-small"
      %td{style:"width:100px;"}= link_to 'Destroy', company, :confirm => 'Are you sure?', :method => :delete, class:" btn btn-danger btn-small"

%br

= link_to 'New Company', new_company_path, class:"btn btn-inverse btn-small"
